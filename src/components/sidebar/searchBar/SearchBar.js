import React from "react";
import classes from "./searchBar.module.scss";

const SearchBar = () => {
  return <div className={classes.searchBar}>SearchBar</div>;
};

export default SearchBar;
