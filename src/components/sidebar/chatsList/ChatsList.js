import React from "react";
import classes from "./chatsList.module.scss";

const ChatsList = () => {
  return <div className={classes.chatsList}>ChatsList</div>;
};

export default ChatsList;
